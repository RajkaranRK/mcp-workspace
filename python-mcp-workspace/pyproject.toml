[project]
name = "python-mcp-workspace"
version = "1.0.0"
description = "Python MCP servers for MongoDB and local filesystem access"
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.0.0",
  "pymongo>=4.0.0"
]

[tool.pyright]
venvPath = "."
venv = ".venv"
